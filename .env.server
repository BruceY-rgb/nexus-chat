# 数据库配置（使用 localhost因为你直接在服务器上运行）
DB_HOST=localhost
DB_PORT=5432
DB_USER=yangsmac
DB_PASSWORD=your_secure_password_here
DB_NAME=slack_chat
DATABASE_URL=postgresql://yangsmac:your_secure_password_here@localhost:5432/slack_chat

# JWT 配置
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long
JWT_EXPIRES_IN=7d

# 邮件配置
RESEND_API_KEY=re_your_resend_api_key
EMAIL_FROM="Slack聊天应用 <onboarding@resend.dev>"
EMAIL_REPLY_TO=onboarding@resend.dev
APP_NAME=Slack聊天应用

# 会话配置
SESSION_SECRET=your-super-secret-session-key-minimum-32-characters-long

# OSS 配置
OSS_REGION=oss-cn-hangzhou
OSS_ACCESS_KEY_ID=your_oss_access_key_id
OSS_ACCESS_KEY_SECRET=your_oss_access_key_secret
OSS_BUCKET=q-and-a-chatbot
OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com

# 应用配置
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_APP_URL=http://slack-chat.ontuotu.com

# WebSocket 配置
WEBSOCKET_PORT=3001

# 功能开关
ENABLE_FILE_UPLOAD=true
ENABLE_SEARCH=true
ENABLE_NOTIFICATIONS=true
ENABLE_EMAIL_NOTIFICATIONS=false

# 文件上传限制
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp,application/pdf,text/plain

# 速率限制
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 日志
LOG_LEVEL=info
LOG_FORMAT=dev
